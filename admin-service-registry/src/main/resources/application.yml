spring:
  port: 8761
  application:
    name: admin-service-registry

eureka:
  instance:
    hostname: localhost
  client:
    fetch-registry: false
    register-with-eureka: false
    service-url: http://${eureka.instance.hostname}:${spring.port}/eureka/


